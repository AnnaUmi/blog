import { render } from '@testing-library/react';
import App from './App';
// this project has no tests, so this is just a placeholder test
test('renders learn react link', () => {
  render(<App />);
});
